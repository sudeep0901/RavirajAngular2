import { MobileHideDirective } from './mobile-hide.directive';

describe('MobileHideDirective', () => {
  it('should create an instance', () => {
    const directive = new MobileHideDirective();
    expect(directive).toBeTruthy();
  });
});
